APP_NAME=AMARU
APP_ENV=local
APP_KEY=base64:oSgwTVZyJ2o8/GUKzF88pATDZmQKIjsWrLHYUL7LWYY=
APP_DEBUG=true
APP_URL=https://reynolds-meaningful-ships-oral.trycloudflare.com

# When exposing the app via a tunnel, set ASSET_URL to the same public URL so
# generated asset() links use the tunnel domain (prevents mixed-content http issues).
ASSET_URL=${APP_URL}

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_ES

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=turismo
DB_USERNAME=root
DB_PASSWORD=


SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
# Set the session cookie domain to the public tunnel host (no scheme).
# This ensures the session cookie is sent by the browser when using the tunnel URL.
SESSION_DOMAIN=reynolds-meaningful-ships-oral.trycloudflare.com

# For tunnel testing via cloudflared: disable secure to allow non-HTTPS testing.
# Cloudflare Quick Tunnels are HTTPS but cloudflared locally is http://localhost.
# Setting to false allows cookies to be sent in both http and https contexts.
SESSION_SECURE_COOKIE=false

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=465
MAIL_ENCRYPTION=ssl
MAIL_USERNAME=moralescadenac.work@gmail.com
MAIL_PASSWORD=eoftobbyrrrkypdd
MAIL_FROM_ADDRESS="moralescadenac.work@gmail.com"
MAIL_FROM_NAME="Amaru"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
